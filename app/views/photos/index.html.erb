<h1>Photos</h1>

<ul>
        <% @photos.each do |photo| %>
        <ol><%= link_to image_tag(photo.image.url(:thumb)),photo %></ol>
        <% end %>
</ul>
<p>
        <%= link_to "Take a new picture", new_photo_path %>
</p>

