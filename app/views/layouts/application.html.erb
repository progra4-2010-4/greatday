<!DOCTYPE html>
<html>
<head>
  <title>Greatday</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= javascript_include_tag 'webcam'%>
  <%= csrf_meta_tag %>
</head>
<body>
      <header>
	<h1><%=link_to "Greatday", root_path%></h1>
        <nav>
        <%if current_user?%>
           <%= image_tag current_user.picture_url %>
           <%= link_to current_user.name,  current_user.account_url %>
           <%= link_to "Logout", "/logout" %>
        <%else%>
           <%= link_to "Login", "/login" %>
          <%end%>
          </nav>
       </header>
     <%= yield %>
	<footer> Greatday 2010. Una aplicación divertida</footer>

</body>
</html>
